import { CardHeaderPropsType, CardPropsType } from './Card';

const base: CardPropsType = {

};

export const mockCardProps = {
  base,
};

const headerBase: CardHeaderPropsType = {
  title: "Card Header"
};

export const mockCardHeaderProps = {
  base: headerBase,
};